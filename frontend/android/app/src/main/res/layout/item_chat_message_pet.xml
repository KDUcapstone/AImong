<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="top"
    android:orientation="horizontal"
    android:paddingHorizontal="12dp"
    android:paddingVertical="6dp">

    <FrameLayout
        android:layout_width="@dimen/pet_sprite_chat_bubble"
        android:layout_height="@dimen/pet_sprite_chat_bubble"
        android:background="@drawable/bg_chat_pet_avatar">

        <ImageView
            android:id="@+id/iv_pet_avatar_sprite"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitCenter"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tv_pet_avatar_emoji"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textSize="30sp"
            android:visibility="gone"
            tools:text="🐣"
            tools:visibility="visible" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_chat_bubble_pet"
            android:lineSpacingMultiplier="1.15"
            android:maxWidth="280dp"
            android:paddingHorizontal="14dp"
            android:paddingVertical="12dp"
            android:textColor="@color/quiz_text_primary"
            android:textSize="15sp"
            tools:text="안녕! 궁금한 걸 편하게 물어봐!" />

        <ImageView
            android:id="@+id/iv_generated_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:adjustViewBounds="true"
            android:background="@drawable/bg_chat_bubble_pet"
            android:contentDescription="@string/chat_image_mode_toggle"
            android:maxWidth="280dp"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:visibility="gone"
            tools:src="@drawable/ic_pet_placeholder"
            tools:visibility="visible" />
    </LinearLayout>
</LinearLayout>
