spring:
  datasource:
    url: ${SUPABASE_DB_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres}
    username: ${SUPABASE_DB_USERNAME:postgres.uuipyygaenzyceebrena}
    password: ${SUPABASE_DB_PASSWORD}
