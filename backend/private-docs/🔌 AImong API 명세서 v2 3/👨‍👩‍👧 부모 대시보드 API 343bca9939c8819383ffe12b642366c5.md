# 👨‍👩‍👧 부모 대시보드 API

**v2.0 반영**: 부모 통계의 완료 수는 `setId` 기준 학습 세트 완료 수로 계산합니다. `incorrectRate`는 최종 submit으로 저장된 `mission_answer_results`만 기준으로 계산하며, 단일 문항 check 응답은 직접 반영하지 않습니다.

> ℹ️ **모든 엔드포인트는 PARENT 인증(Firebase ID Token)이 필요합니다.**
> 

> Firebase 토큰 → `parent_accounts.parent_id`(Firebase UID, String) 확인 → `child_profiles.parent_id` 일치 여부 검증 (불일치 시 403)
> 

## GET /parent/child/{childId}/summary

> 자녀 학습 요약 (스트릭 + XP + 미션 완료 수)
> 

**Response 200**

```json
{
  "success": true,
  "data": {
    "nickname": "민준",
    "profileImageType": "SPROUT",
    "totalXp": 150,
    "continuousDays": 5,
    "shieldCount": 2,
    "weeklyCompletedSetCount": 7,
    "totalCompletedSetCount": 12,
    "currentLevelNo": 2,
    "lastActiveAt": "2026-03-29T09:30:00Z"
  }
}
```

> **v1.6**: `lastActiveAt`는 `child_profiles.last_active_at` 기준. CHILD 인증 API 호출 성공 시마다 갱신.
> 

**집계 기준**

- `weeklyCompletedSetCount`, `totalCompletedSetCount`: `mission_attempts.is_review = false`, `is_passed = true`, `set_id` 최초 완료 기준
- 기존 `weeklyMissionCount`, `totalMissionCount` 필드를 유지해야 하면 의미를 "완료한 학습 세트 수"로 문서화합니다.

---

## GET /parent/child/{childId}/weekly-stats

> 주간 학습 통계 (막대 그래프용 — 월~일 7일 데이터)
> 

**Response 200**

```json
{
  "success": true,
  "data": {
    "weekStart": "2026-03-23",
    "weekEnd": "2026-03-29",
    "totalWeeklyXp": 85,
    "totalWeeklyMissions": 7,
    "dailyStats": [
      { "date": "2026-03-23", "dayOfWeek": "MON", "completedSetCount": 2, "xpEarned": 20 },
      { "date": "2026-03-24", "dayOfWeek": "TUE", "completedSetCount": 0, "xpEarned": 0  },
      { "date": "2026-03-25", "dayOfWeek": "WED", "completedSetCount": 3, "xpEarned": 35 },
      { "date": "2026-03-26", "dayOfWeek": "THU", "completedSetCount": 1, "xpEarned": 10 },
      { "date": "2026-03-27", "dayOfWeek": "FRI", "completedSetCount": 1, "xpEarned": 10 },
      { "date": "2026-03-28", "dayOfWeek": "SAT", "completedSetCount": 0, "xpEarned": 0  },
      { "date": "2026-03-29", "dayOfWeek": "SUN", "completedSetCount": 0, "xpEarned": 0  }
    ]
  }
}
```

---

## GET /parent/child/{childId}/privacy-log

> 개인정보 감지 이력 조회
> 

> 🆕 **v1.5 추가**: `page`, `size` 쿼리 파라미터 지원 권장. MVP 기본값 `page=0`, `size=20`
> 

**Response 200**

```json
{
  "success": true,
  "data": {
    "page": 0,
    "size": 20,
    "totalCount": 5,
    "hasNext": false,
    "weeklyCount": 2,
    "events": [
      { "detectedType": "NAME",   "masked": true,  "detectedAt": "2026-03-29T10:00:00Z" },
      { "detectedType": "SCHOOL", "masked": false, "detectedAt": "2026-03-27T15:30:00Z" }
    ]
  }
}
```

> **v1.6**: `weeklyCount`는 이번 주 월요일(KST) 00:00 이후 발생 건수 기준
> 

> ⚠️ 원문 텍스트 절대 반환하지 않음. 감지 유형(detectedType)만 반환.
> 

---

## GET /parent/child/{childId}/weak-points

> 자주 틀리는 **학습 세트/교육 주제 분석** (최근 30일)
> 

> 🆕 **v1.1 수정**: 문제 유형 분석이 아닌 **미션 단위 분석**. `page`, `size` 지원 권장.
> 

**Response 200**

```json
{
  "success": true,
  "data": {
    "page": 0,
    "size": 20,
    "totalCount": 2,
    "hasNext": false,
    "analyzedPeriod": "최근 30일",
    "weakPoints": [
      {
        "missionId": "uuid",
        "missionTitle": "팩트체크란?",
        "setId": "S0301-L4",
        "setTitle": "팩트체크 판단하기",
        "levelNo": 4,
        "difficulty": "MEDIUM",
        "stage": 3,
        "incorrectRate": 0.6,
        "attemptCount": 5
      },
      {
        "missionId": "uuid",
        "missionTitle": "개인정보 보호",
        "stage": 2,
        "incorrectRate": 0.4,
        "attemptCount": 3
      }
    ]
  }
}
```

> **v1.6**: `incorrectRate`는 최근 30일 동안 해당 미션의 전체 답안(복습 포함) 기준 `문항 단위 오답 수 / 전체 문항 응답 수`
> 

> **v2.0**: 전체 답안은 최종 submit으로 저장된 `mission_answer_results`만 의미합니다. 단일 문항 check 응답은 학습 중 피드백용이며 부모 통계에 직접 반영하지 않습니다. `weakPoints`는 `setId`, `setTitle`, `difficulty`를 함께 반환해 같은 교육 주제 안의 레벨별 약점을 구분합니다.
> 

**예외 처리**

| 상황 | HTTP | code | message |
| --- | --- | --- | --- |
| 분석 데이터 없음 | 200 | - | weakPoints: [] 빈 배열 반환 |

---

## 🔧 Spring Boot 구현 가이드

> ⚠️ **v2.0 구현 시 주의**: 아래 코드 예시의 `weeklyMissionCount`, `totalMissionCount`, `missionId` 집계 메서드는 하위 호환 이름으로 볼 수 있습니다. 실제 쿼리는 `COUNT(DISTINCT set_id)`와 `mission_answer_results.set_id` 기준으로 작성해야 합니다.
> 

### 부모 소유권 검증 패턴

> 등 4개 엔드포인트 모두 동일한 인증 흐름을 사용. Controller에서 수동 호출하지 말고 **AuthFilter에서 주입된 parentId**를 실제로 사용할 것.
> 

```java
// AuthFilter에서 parentId(Firebase UID, String)를 RequestAttribute로 주입하면 Controller는 다음처럼
@RestController
@RequiredArgsConstructor
public class ParentDashboardController {

    private final ParentDashboardService dashboardService;

    @GetMapping("/parent/child/{childId}/summary")
    public ApiResponse<SummaryResponse> getSummary(
            @PathVariable UUID childId,
            @RequestAttribute("parentId") String parentId
    ) {
        return ApiResponse.success(dashboardService.getSummary(parentId, childId));
    }

    @GetMapping("/parent/child/{childId}/weekly-stats")
    public ApiResponse<WeeklyStatsResponse> getWeeklyStats(
            @PathVariable UUID childId,
            @RequestAttribute("parentId") String parentId
    ) {
        return ApiResponse.success(dashboardService.getWeeklyStats(parentId, childId));
    }

    @GetMapping("/parent/child/{childId}/privacy-log")
    public ApiResponse<PrivacyLogResponse> getPrivacyLog(
            @PathVariable UUID childId,
            @RequestAttribute("parentId") String parentId,
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "20") int size
    ) {
        return ApiResponse.success(dashboardService.getPrivacyLog(parentId, childId, page, size));
    }

    @GetMapping("/parent/child/{childId}/weak-points")
    public ApiResponse<WeakPointsResponse> getWeakPoints(
            @PathVariable UUID childId,
            @RequestAttribute("parentId") String parentId,
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "20") int size
    ) {
        return ApiResponse.success(dashboardService.getWeakPoints(parentId, childId, page, size));
    }
}
```

### Service 로직

```java
@Service
@RequiredArgsConstructor
public class ParentDashboardService {

    private final ChildProfileRepository childRepo;
    private final MissionAttemptRepository missionAttemptRepo;
    private final MissionAnswerResultRepository answerResultRepo;
    private final PrivacyEventRepository privacyEventRepo;
    private final StreakRecordRepository streakRepo;

    // ─── 공통: 소유권 검증 (등 4개 엔드포인트에서 호출) ───
    private ChildProfile validateOwnership(String parentId, UUID childId) {
        ChildProfile child = childRepo.findById(childId)
            .orElseThrow(() -> new AImongException(ErrorCode.NOT_FOUND));
        if (!child.getParentId().equals(parentId)) {
            throw new AImongException(ErrorCode.FORBIDDEN); // 403
        }
        return child;
    }

    // ─── GET /summary ────────────────────────────
    public SummaryResponse getSummary(String parentId, UUID childId) {
        ChildProfile child = validateOwnership(parentId, childId);
        StreakRecord streak = streakRepo.findById(childId).orElseThrow();

        LocalDate weekStart = LocalDate.now(ZoneId.of("Asia/Seoul")).with(DayOfWeek.MONDAY);
        Instant weekStartInstant = weekStart.atStartOfDay(ZoneId.of("Asia/Seoul")).toInstant();

        // is_review=false, is_passed=true, set_id별 최초 완료만 집계
        int weeklyMissionCount = missionAttemptRepo.countWeeklyFirstCompletedMissions(childId, weekStartInstant);
        int totalMissionCount = missionAttemptRepo.countTotalFirstCompletedMissions(childId);

        return SummaryResponse.builder()
            .nickname(child.getNickname())
            .profileImageType(child.getProfileImageType())
            .totalXp(child.getTotalXp())
            .continuousDays(streak.getContinuousDays())
            .shieldCount(child.getShieldCount())
            .weeklyMissionCount(weeklyMissionCount)
            .totalMissionCount(totalMissionCount)
            .lastActiveAt(child.getLastActiveAt())
            .build();
    }

    // ─── GET /weekly-stats ──────────────────────────
    public WeeklyStatsResponse getWeeklyStats(String parentId, UUID childId) {
        validateOwnership(parentId, childId);

        LocalDate today = LocalDate.now(ZoneId.of("Asia/Seoul"));
        LocalDate weekStart = today.with(DayOfWeek.MONDAY);
        LocalDate weekEnd = today.with(DayOfWeek.SUNDAY);

        Instant from = weekStart.atStartOfDay(ZoneId.of("Asia/Seoul")).toInstant();
        Instant to = weekEnd.plusDays(1).atStartOfDay(ZoneId.of("Asia/Seoul")).toInstant();

        // JPQL: mission_attempts를 KST 날짜 기준으로 GROUP BY
        List<DailyStatDto> rawStats = missionAttemptRepo
            .findDailyStatsByChildIdBetween(childId, from, to);

        // 주중 7일 모두 마마 포함 (missing 일짜는 0으로 보완)
        List<DailyStatResponse> dailyStats = weekStart.datesUntil(weekEnd.plusDays(1))
            .map(date -> {
                DailyStatDto dto = rawStats.stream()
                    .filter(d -> d.getDate().equals(date))
                    .findFirst().orElse(DailyStatDto.empty(date));
                return DailyStatResponse.of(date, dto);
            }).toList();

        return WeeklyStatsResponse.builder()
            .weekStart(weekStart)
            .weekEnd(weekEnd)
            .totalWeeklyXp(dailyStats.stream().mapToInt(DailyStatResponse::getXpEarned).sum())
            .totalWeeklyMissions(dailyStats.stream().mapToInt(DailyStatResponse::getMissionCount).sum())
            .dailyStats(dailyStats)
            .build();
    }

    // ─── GET /privacy-log ─────────────────────────
    public PrivacyLogResponse getPrivacyLog(String parentId, UUID childId, int page, int size) {
        validateOwnership(parentId, childId);

        Instant weekStartInstant = LocalDate.now(ZoneId.of("Asia/Seoul"))
            .with(DayOfWeek.MONDAY)
            .atStartOfDay(ZoneId.of("Asia/Seoul")).toInstant();

        Pageable pageable = PageRequest.of(page, size,
            Sort.by(Sort.Direction.DESC, "detectedAt"));
        Page<PrivacyEvent> eventPage = privacyEventRepo.findByChildId(childId, pageable);

        long weeklyCount = privacyEventRepo.countByChildIdAndDetectedAtAfter(childId, weekStartInstant);

        List<PrivacyLogItem> events = eventPage.getContent().stream()
            .map(e -> new PrivacyLogItem(
                e.getDetectedType(),
                e.isMasked(),
                e.getDetectedAt()
            )).toList();

        return new PrivacyLogResponse(
            page,
            size,
            eventPage.getTotalElements(),
            eventPage.hasNext(),
            weeklyCount,
            events
        );
    }

    // ─── GET /weak-points ─────────────────────────
    public WeakPointsResponse getWeakPoints(String parentId, UUID childId, int page, int size) {
        validateOwnership(parentId, childId);

        Instant since = Instant.now().minus(30, ChronoUnit.DAYS);
        Pageable pageable = PageRequest.of(page, size);

        // 최종 submit으로 저장된 mission_answer_results 기반 문항 단위 오답률 계산 (복습 포함, check 결과 제외)
        Page<WeakPointDto> weakPoints = answerResultRepo.findWeakPointsByChildId(childId, since, pageable);

        return new WeakPointsResponse(
            page,
            size,
            weakPoints.getTotalElements(),
            weakPoints.hasNext(),
            "최근 30일",
            weakPoints.getContent()
        );
    }
}
```

### Repository 쿼리

```java
public interface MissionAttemptRepository extends JpaRepository<MissionAttempt, UUID> {

    @Query("""
        SELECT COUNT(DISTINCT a.missionId)
        FROM MissionAttempt a
        WHERE a.childId = :childId
          AND a.isReview = false
          AND a.isPassed = true
        """)
    int countTotalFirstCompletedMissions(@Param("childId") UUID childId);

    @Query("""
        SELECT COUNT(DISTINCT a.missionId)
        FROM MissionAttempt a
        WHERE a.childId = :childId
          AND a.isReview = false
          AND a.isPassed = true
          AND a.createdAt >= :after
        """)
    int countWeeklyFirstCompletedMissions(@Param("childId") UUID childId, @Param("after") Instant after);

    // 공식: KST 일자 기준 GROUP BY (created_at = 제출 완료 시각)
    @Query("""
        SELECT CAST(CONVERT_TZ(a.createdAt, 'UTC', 'Asia/Seoul') AS DATE) AS date,
               COUNT(a.attemptId)     AS missionCount,
               SUM(a.xpEarned)        AS xpEarned
        FROM MissionAttempt a
        WHERE a.childId = :childId
          AND a.isReview = false
          AND a.isPassed = true
          AND a.createdAt BETWEEN :from AND :to
        GROUP BY CAST(CONVERT_TZ(a.createdAt, 'UTC', 'Asia/Seoul') AS DATE)
        """, nativeQuery = false)
    List<DailyStatDto> findDailyStatsByChildIdBetween(
        @Param("childId") UUID childId,
        @Param("from") Instant from,
        @Param("to") Instant to
    );
}

public interface MissionAnswerResultRepository extends JpaRepository<MissionAnswerResult, UUID> {

    // 미션별 incorrectRate 계산 — 최종 submit 결과만 기준, 복습(is_review=true) 포함, check 결과 제외
    @Query("""
        SELECT new com.aimong.dto.WeakPointDto(
            m.missionId,
            m.title,
            m.stage,
            CAST(SUM(CASE WHEN r.isCorrect = false THEN 1 ELSE 0 END) AS double) / COUNT(r.resultId),
            COUNT(DISTINCT r.attemptId)
        )
        FROM MissionAnswerResult r
        JOIN Mission m ON r.missionId = m.missionId
        WHERE r.childId = :childId
          AND r.createdAt >= :since
        GROUP BY m.missionId, m.title, m.stage
        HAVING COUNT(r.resultId) > 0
        ORDER BY CAST(SUM(CASE WHEN r.isCorrect = false THEN 1 ELSE 0 END) AS double) / COUNT(r.resultId) DESC
        """)
    Page<WeakPointDto> findWeakPointsByChildId(
        @Param("childId") UUID childId,
        @Param("since") Instant since,
        Pageable pageable
    );
}

public interface PrivacyEventRepository extends JpaRepository<PrivacyEvent, UUID> {
    Page<PrivacyEvent> findByChildId(UUID childId, Pageable pageable);
    long countByChildIdAndDetectedAtAfter(UUID childId, Instant after);
}
```

### ⚠️ 주의사항

| 항목 | 설명 |
| --- | --- |
| 403 소유권 검증 | **모든 엔드포인트**에서 `child.getParentId().equals(parentId)` 확인 필수. 세션 토큰으로 받은 parentId를 신뢰. |
| 원문 텍스트 누설 | `privacy_log` 응답에 `original_text` 절대 포함 금지. `detected_type`만 반환. |
| KST GROUP BY | MySQL `CONVERT_TZ(ts, 'UTC', 'Asia/Seoul')` 활용. JVM 타임존만으로 해결 불가. |
| weekly-stats 빈 날짜 보완 | DB에 데이터 없는 날짜는 Java에서 0으로 비워서 후 응답. 항상 7일 모두 포함. |
| incorrectRate 계산 | v2.0: 최근 30일, 최종 submit으로 저장된 전체 시도(is_review 포함), 문항 단위 오답 수 / 전체 문항 응답 수. `mission_answer_results.set_id`를 근거로 세트별 약점까지 계산하며 check 결과는 제외. |
| lastActiveAt 갱신 | CHILD 인증 API가 2xx로 성공한 뒤 `child_profiles.last_active_at = NOW()` 갱신. 필터 선처리보다 interceptor/post-handler 또는 서비스 성공 지점에서 처리 권장. |